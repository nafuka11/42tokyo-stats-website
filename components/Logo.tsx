import { Typography } from "@mui/material";

const Logo = () => {
  return (
    <Typography sx={{ fontWeight: 500, color: "#444" }} variant="h6">
      42Tokyo Stats
    </Typography>
  );
};

export default Logo;
